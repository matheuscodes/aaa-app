export default function FloatingActionButtonStyle(style) {

  return {
    width: `${style.baseFontsize * 2}px`,
    height: `${style.baseFontsize * 2}px`,
    verticalAlign: `middle`,
    minWidth: null,
    iconStyle: {
      width: `${style.baseFontsize * 2}px`,
      height: `${style.baseFontsize * 2}px`,
      minWidth: null,
    }
  }

}
